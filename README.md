# springBootApi

This is a sample Spring Boot application that demonstrates how to create and enable API endpoints in Java. The controller supports standard HTTP methods, including GET, PUT, DELETE, and POST.

The API connects to a PostgreSQL database, with configuration details specified in the application.yaml file.
